import styled from 'styled-components';

const FooterWrapper = styled.footer`
  color: gray;
  font-size: var(--fs-xs);
  padding: 1rem;
  text-align: center;
`;

export default FooterWrapper;